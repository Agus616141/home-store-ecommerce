# Evita el bug de pnpm `runDepsStatusCheck` al correr scripts dentro de un
# workspace con dependencias `workspace:*` (apps/web depende de @homestore/types).
# Sin esto, `pnpm --filter ... <script>` falla con ERR_PNPM_WORKSPACE_PKG_NOT_FOUND.
verify-deps-before-run=false
